#include <stdio.h>

int main() {
    int num = 0;
    int seebal = 0;
    int fuck[1000] = { '\n' };
    printf("> ");
    scanf("%d", &num);
    
    for (int i = 0; i < num; i++) {
        scanf("%d", &fuck[i]);
    }
    
    for (int i = 0; i < num; i++) {
        for ( int j = 0; j < num; j++) {
                
        }
        printf("\n");
    }
    
    return 0;
    
}
